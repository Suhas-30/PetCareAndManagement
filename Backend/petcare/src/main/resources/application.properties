spring.application.name=PetCare

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update


# ---------- MAIL CONFIG ----------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=suhashs1813688815@gmail.com
spring.mail.password=neohldqhukutldeo

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.mail.from=smartpetcareorg@gmail.com


# ---------- OTHER ----------
app.otp.resend-cooldown-seconds=60


jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

razorpay.key=${TEST_KEY}
razorpay.secret=${TEST_SECRET}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

google.calendar.credentials-path=${GOOGLE_CALENDAR_CREDENTIALS_PATH}
google.calendar.calendar-id=${GOOGLE_CALENDAR_ID}

google.oauth2.client-id=${GOOGLE_CLIENT_ID}
google.oauth2.client-secret=${GOOGLE_CLIENT_SECRET}
google.oauth2.redirect-uri=${GOOGLE_REDIRECT_URI}